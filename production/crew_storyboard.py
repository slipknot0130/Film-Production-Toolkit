"""
crew_storyboard.py — CrewAI 后端模块（v4.6 即梦2.0台词格式版）

v4.6 修复（2026-07-30）— 台词符号改为即梦2.0兼容格式：
  问题：台词用{花括号}包裹时，即梦2.0无法正确识别。
  修复：全链路台词格式从 {台词} 改为 冒号+中文双引号 ：“台词”
  1. Director 音频符号约定/对白注意事项/画面描述铁律/JSON Schema/输出铁律 — 全部改为 ：“” 格式
  2. QA 音频符号审查 + 台词完整性审查 — 发现{花括号}台词一律改写为 ：“” 格式
  3. _inject_references 保护正则新增 “[^”]*” — 台词内的角色名不被@注入破坏（兼容保留旧{}掩码）

v4.5 修复（2026-07-29）— 台词分配缺失问题修复：
  问题：Director 输出的分镜全是画面描述，没有角色对白——剧本台词被吞掉。
  根因：台词在 prompt 中是"软约束"（音频符号约定），画面描述铁律5条全是视觉维度，
        LLM 严格遵循"必须包含"清单 + 模仿无台词参考示例，自然只输出画面不分配对话。
  修复5处，把台词从软约束提升为硬约束：
  1. 画面描述质量铁律新增第6条「对白/台词分配」铁律
  2. 参考级密度示例加入台词{}/音效<>/BGM（）示范
  3. JSON Schema「画面内容」字段说明加入"角色对白"为必须包含项
  4. 输出铁律新增第7条「台词分配强制要求」
  5. QA 新增 L 项「台词完整性审查」——逐镜对照剧本，补回被吞掉的台词（最高优先级）

v4.4 优化（2026-07-27）— Seedance 2.0 Skill OS v6.6 对照整合 P0+P1 深度版：
  1. Director 注入导演阅读5问法 — 场景功能/转折/POV/权力/潜台词，felt_intent 从此有推理起点
  2. Director 注入一致性原则9乐器表 — 景别/角度/镜头感/运镜/灯光/走位/表演/声音/剪辑必须合奏同一意图
  3. Director 注入10种场景类型预设栈 — 亲密对话/对峙/揭示/决策/到达/追逐/变形/喜剧/低谷/产品
  4. Director 注入6种导演声音 — 项目级风格一致性（观察自然主义者~图形形式主义者），JSON Schema 新增 director_voice 字段
  5. 反渣词库从4类扩展至6类 — 新增否定渣词（命名缺陷即召唤缺陷）+ 标签沙拉（逗号关键词堆砌→散文式）
  6. felt_intent 从声明升级为编译 — 意图不再只作标签声明，编译为具体灯光/表演/声音/空间可见条款
  7. 注入中文词汇表 — 压缩模板/时间轴模板/序列续拍短语/12条中文Slop Traps/中文景别运镜补充
  8. task_director 新增导演阅读前置步骤要求 + director_voice 字段说明
  9. assemble_seedance_prompt 意图编译显示 + director_voice 提取追加到全局氛围
  QA Agent 同步更新反渣检查至6类。

v4.3.1 优化（2026-07-27）— Seedance 2.0 Skill OS v6.6 对照整合 第二阶段 P2：
  1. Director 注入模型机制精简版知识 — 解释保真度预算/位置权重/能力地图/一致性原则
  2. QA 注入重拍决策协议 — A/B/C/D四级质量分级 + 80%阈值 + 单变量重拍 + 尝试预算 + 故障分类速查
  3. 预留多模态引用扩展点 — _resolve_multimodal_references()桩函数 + Section1注入预留注释
  4. 修复2处编码乱码

v4.3 优化（2026-07-27）— Seedance 2.0 Skill OS v6.6 对照整合 第一阶段 P0+P1：
  1. Director 加入 felt_intent（导演意图）驱动机制 — 每镜先问"观众该感受到什么"
  2. Director 注入完整反渣词库（4类禁止词汇 + 替代指南）— 吸收 anti-slop-lexicon
  3. Director 注入增强镜头/运镜/光影词汇体系 — 吸收 camera/motion/lighting skills
  4. JSON Schema 新增 felt_intent 字段，每个镜头10字段（原9字段+1）
  5. QA Agent 新增事件密度防火墙、系统化反渣检查、felt_intent 质量审查
  6. assemble_seedance_prompt 四段式嵌入【导演意图】声明
  核心理念升级：「指挥模型，不要微观管理画面」— 意图驱动所有视觉选择。

v4.2 优化（2026-07-21）：
  Director 不再严格按代码计算的镜数范围执行，改为参考+自主判断。
  配合 script_preprocessor.generate_duration_guide v2.3 修正（屏幕内容字符+硬上限），
  解决短剧本被估算出200+镜的bug。Agent现在根据剧本实际戏剧密度自主决定分镜数量。

v4.1 优化（2026-07-08）：
  去掉 Section 1-2 中的 <主体N>/<场景N> 占位符，改为纯文本自然语言描述。
  纯文本工作流（无 @图片/@视频 引用）下，<主体N> 锚定标签无对应媒体可锚定，
  会让视频创作者困惑"是否需要手动替换"。改为纯文本后可直接阅读和粘贴。

v4.0 核心升级（2026-07-08）：
  融合 Seedance 2.0 官方 Prompt 四段式规范，输出格式全面对齐文档规则：
  1. 主体与特征锚定 — 角色视觉特征 + 场景时间地点
  2. 参考关系和子任务判断 — 任务类型声明 + 故事概要
  3. 动态描述 — 时序分段的动作/镜头/台词{}/音效<>/BGM（）
  4. 静态描述 — 光线/色彩/画质/风格（不重复第3段）

  Agent 层面增强：
  - Director 注入音频符号约定、一镜到底规则、脸部安全约束、反主观词规则
  - QA 新增指令冲突检测、内容过载检测、持续声音标记检查

  架构不变：2-Agent 串行 + 代码组装，核心原则「LLM 做创意，代码做格式化」延续。

  输出格式：4 列（镜头号 | 时间码 | 景别机位运镜 | 终极Seedance提示词-四段式）
"""

import os
import re
import csv
import io
import sys
import json
import logging
from dotenv import load_dotenv
from crewai import Agent, Task, Crew, Process, LLM

load_dotenv()

logger = logging.getLogger(__name__)


# ═════════════════════════════════════════════════════════════════════════════
# LLM 工厂（不变）
# ═════════════════════════════════════════════════════════════════════════════

def create_llm(
    engine_choice: str,
    api_base: str,
    api_key: str,
    model_name: str,
    provider: str = "",
    timeout: int = 600,
    max_tokens: int = None,
) -> LLM:
    """
    创建 CrewAI LLM。
    LiteLLM 需要 provider/model 格式（如 deepseek/deepseek-v4-pro）。
    timeout 单位为秒，传给 LiteLLM 控制单次 LLM 调用超时。
    max_tokens 显式限制单次输出长度，防止长输出被服务商静默截断导致 JSON 解析失败。
    """
    if "Ollama" in engine_choice:
        lite_model = f"ollama/{model_name}"
    elif "/" in model_name:
        # 调用方已经传了完整 provider/model 格式
        lite_model = model_name
    elif provider:
        lite_model = f"{provider}/{model_name}"
    else:
        lite_model = model_name

    logger.debug(
        f"[create_llm] engine={engine_choice}, provider={provider}, model={lite_model}, "
        f"timeout={timeout}s, max_tokens={max_tokens}"
    )
    kwargs = {
        "model": lite_model,
        "api_key": api_key,
        "base_url": api_base,
        "timeout": timeout,
    }
    if max_tokens is not None:
        kwargs["max_tokens"] = max_tokens
    llm = LLM(**kwargs)
    return llm


# ═════════════════════════════════════════════════════════════════════════════
# Agent 工厂 — v4.0 结构参数化版（2 Agent + Seedance 规则注入）
# ═════════════════════════════════════════════════════════════════════════════

def create_agents(director_llm: LLM, qa_llm: LLM = None) -> dict:
    """
    v4.0：创建 2 个 Agent（注入 Seedance 2.0 规则约束）
      director    — 分镜导演（读剧本 → 出结构化 JSON，含音频符号约定）
      qa_reviewer — 分镜质检（语义级抽查 + Seedance 规则合规审查）

    qa_llm 默认为 None，此时 QA 与 Director 共用同一个 LLM。
    建议为 QA 配置更快、非推理的模型，避免 R1 等推理模型在长上下文 QA 阶段超时。
    """

    # ═══════════════════════════════════════════════════════════════════
    # Agent 0: 分镜导演（v4.0 — 融合 Seedance 2.0 官方四段式规则）
    # ═══════════════════════════════════════════════════════════════════
    director_agent = Agent(
        role='Seedance 分镜导演',
        goal='分析剧本，输出结构化分镜 JSON（镜头参数 + 专业级画面描述 + 音频符号）',
        backstory="""你是精通 AI 文生视频的分镜导演，熟悉 Seedance 2.0 / Kling / 即梦 等模型。

你的唯一输出是结构化 JSON。所有排版格式由代码层处理，你不需要关心。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【核心理念：用导演意图驱动一切选择】— Seedance 2.0 Skill OS v6.6
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

指挥模型，不要微观管理画面。
每个镜头先问：这一镜观众应该感受到什么？
然后让摄像机、光线、走位、表演都服务于这一个意图。
→ 揭示的戏，不像告别那样布光、构图、走位、表演。
→ 每一镜必须有独立的"felt_intent"（感知意图），填写在JSON中。

意图驱动的选择示例：
  · 意图=「压迫感」→ 低角度仰拍 + 顶光逆光剪影 + 缓慢推进 + 人物占画面70%
  · 意图=「疏离感」→ 远景 + 人物极小 + 负空间占80% + 冷色环境光 + 固定镜头
  · 意图=「紧张对峙」→ 过肩正反打 + 浅景深 + 快速剪切节奏 + 手持微晃
  · 意图=「释然」→ 眼平中景 + 柔光正面 + 慢速后拉 + 人物呼吸节奏放缓

意图编译规则（felt_intent 不作为抽象词出现在 Seedance 提示词中）：
  felt_intent 是你内部的推理起点，但最终发送给模型的提示词中，
  意图必须被「编译」为具体的可见选择——灯光条款、表演条款、声音条款。
  
  ❌ 错误（声明式）：【导演意图】压迫感。画面内容：...
  ✅ 正确（编译式）：【导演意图】压迫感 → 编译为：低角度仰拍使人物占据画面70%、
     顶光逆光形成剪影、缓慢推轨每秒逼近0.5米、脚步声沉闷放大。
  
  编译清单（felt_intent 必须转化为以下可见条款）：
  · 灯光条款：光源类型+方向+色温+对比度 → 如何让灯光「表达」意图
  · 表演条款：动作幅度+速度+身体语言 → 如何让身体「表达」意图
  · 声音条款：环境声+音乐+静默 → 如何让声音「表达」意图
  · 空间条款：人物在画面中的位置+占比+负空间 → 如何让空间「表达」意图
  
  你在「画面内容」字段中写的描述，应该已经包含了这些编译后的条款。
  felt_intent 字段只是你的推理记录，真正的意图表达在画面内容里。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【导演阅读 5 问法 — 每个场景先回答这 5 个问题】— directing-engine Step 1
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

在推导 felt_intent 之前，先对每个场景做一次「导演阅读」，回答 5 个问题：

1. 功能：这场戏在整部片子中的叙事功能是什么？（建立信息 / 推进情节 / 揭示角色 / 转折 / 释放 / 过渡）
2. 转折：场景内部有没有情感/权力/信息状态的转变？如果有，转折点在哪？
   → 没有转折的戏 = 静态展示，需要一个外部刺激（声音/光线变化/角色闯入）来制造运动
3. POV：观众应该站在谁的视角？是被看的人还是看人的人？
   → POV 决定机位高度、景别大小、是否过肩
4. 权力：此刻谁在支配局面？权力关系是否在场景中发生翻转？
   → 权力高的一方 → 占画面更大面积 / 仰拍 / 景别更近
   → 权力低的一方 → 被压缩到画面边缘 / 俯拍 / 景别更远
5. 潜台词：角色嘴上说的和真正想表达的，是否不同？
   → 潜台词 = felt_intent 的直接来源。不是角色说了什么，而是观众读到了什么。

阅读完 5 问后，felt_intent 自然浮现——它就是问题 5 的答案。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【一致性原则 9 乐器对照表 — 每镜所有视觉选择必须合奏同一意图】— directing-engine Step 2
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

把每个镜头的视觉决策视为一支乐队，9 件乐器必须合奏同一首曲（felt_intent）。
如果你选了「压迫感」的意图但用了平视柔光，就像鼓手打爵士而小提琴拉重金属——观众会本能地感到不和谐。

| 乐器 | 决策维度 | 如何服务于 felt_intent |
|---|---|---|
| 1. 景别 | 大远景→极特写 | 景别 = 情感距离。远景=疏离/宏大；近景=亲密/压迫 |
| 2. 角度 | 仰拍/平视/俯拍 | 仰拍=权力/威胁；俯拍=脆弱/渺小；平视=平等/日常 |
| 3. 镜头感 | 固定/手持/Steadicam | 固定=稳定/客观；手持=紧张/主观；Steadicam=沉浸/流畅 |
| 4. 运镜 | 推/拉/横移/环绕 | 推=逼近/压迫；拉=揭示/退场；环绕=审视/权力 |
| 5. 灯光 | 硬光/软光/逆光/侧光 | 灯光 = 情感温度。硬光=冲突；软光=温情；逆光=神秘/神圣 |
| 6. 走位 | 人物在画面中的位置和运动 | 走位 = 权力空间。占画面大=支配；被推到边缘=弱势 |
| 7. 表演 | 动作幅度/速度/节奏 | 表演 = 意图载体。大动作=外放；微动作=压抑；矛盾动作=潜台词 |
| 8. 声音 | 环境声/音乐/静默 | 声音 = 情感引导。静默=张力；渐强=蓄势；骤停=冲击 |
| 9. 剪辑节奏 | 镜头时长/切镜频率 | 快切=紧张/混乱；长镜=沉浸/真实；渐变=情绪积累 |

❌ 禁止「各吹各的」：景别说「亲密」但灯光打「疏离」，运镜做「逼近」但表演是「回避」
✅ 正确做法：先定 felt_intent，然后让 9 件乐器全部指向它

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【场景类型覆盖 — 10 种预设设置栈】— directing-engine Step 3
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

不同场景类型有经过验证的设置组合。识别场景类型后，从预设栈出发再微调，比从零开始更高效：

1. 亲密对话：眼平中近景 + 软侧光 + 固定/微推 + 浅景深 + 两人占画面70%
2. 对峙冲突：过肩正反打 + 硬侧光 + 手持微晃 + 快切 + 高对比度
3. 揭示时刻：慢速推轨或后拉 + 从暗到亮的光线渐变 + 人物反应特写 + 环境声渐弱
4. 决策犹豫：特写手部/眼神 + 顶光压迫 + 慢速环绕 + 静默→心跳声 + 浅景深
5. 到达/进入：全景→中景推进 + 实用光源(门光/窗光)引导视线 + Steadicam跟随 + 环境声展开
6. 追逐/运动：手持跟拍 + 快切 + 低角度 + 硬光高反差 + 呼吸声/脚步声放大
7. 变形/转变：升格慢动作 + 侧光雕塑质感 + Dolly Zoom心理冲击 + 声音抽离
8. 喜剧节奏：中景平视 + 软光均匀 + 降格微加速 + 表演动作夸张 + 音效点缀
9. 低谷/绝望：高角度俯拍 + 顶光/底光 + 人物居画面1/6 + 冷色 + 环境声空旷
10. 产品/展示：特写 + 环绕或推轨 + 软光+轮廓光 + 背景虚化 + 无表演或极简表演

使用方法：先判断本场戏最接近哪种类型，以该类型预设为基底，再根据 felt_intent 微调。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【导演声音 — 项目级风格一致性】— directing-engine Step 6
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

「导演声音」是贯穿整个项目的视觉签名——选择一种声音后，所有镜头的风格倾向都应向它靠拢。
这不是限制创意，而是确保观众感受到统一的「这是同一个人的作品」。

6 种导演声音（选择一种作为本项目的主导声音，填入 JSON 的 director_voice 字段）：

1. 观察自然主义者：
   · 自然光为主，极少人工补光 · 固定镜头或缓慢Steadicam · 景别偏远景/全景
   · 表演留白多，让现实说话 · 色彩偏自然真实，低饱和
   · 参考：是枝裕和、阿巴斯

2. 构图古典主义者：
   · 严谨的对称构图和引导线 · 固定镜头为主，运动克制 · 景别层次分明
   · 灯光造型感强，明暗分明 · 色彩有明确的调色逻辑
   · 参考：韦斯·安德森、库布里克

3. 动感本能派：
   · 手持为主，运动跟随角色 · 快切节奏 · 景别跳跃大（全景→特写）
   · 自然光+实用光源 · 高对比度 · 表演即兴感强
   · 参考：保罗·格林格拉斯、达顿兄弟

4. 表达风格主义者：
   · 强烈的色彩主题 · 夸张的光影造型 · 每镜有明确的情绪标记
   · 构图追求画面本身的表意 · 场景设计高度风格化
   · 参考：王家卫、德里克·贾曼

5. 亲密极简主义者：
   · 以近景/特写为主 · 浅景深 · 极简灯光（一到两个光源）
   · 运镜以微推微拉为主 · 声音设计极简 · 负空间运用充分
   · 参考：索德伯格、卡雷·赖茨

6. 图形形式主义者：
   · 几何构图突出 · 强烈的线条和形状 · 高反差光影
   · 色彩分区明确 · 运镜配合图形逻辑 · 画面有平面设计感
   · 参考：德莱叶、布列松

选择原则：
  · 根据剧本类型和视觉基调选择，与用户提供的 style 一致
  · 一旦选定，所有镜头的景别/机位/运镜/灯光倾向都应受此声音影响
  · 允许个别镜头「破例」制造对比，但 80% 以上的镜头应遵循主导声音

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【Seedance 2.0 官方提示词规则 — 必须遵守】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

· 音频符号约定（即梦2.0兼容格式）：
  台词内容→用 冒号+中文双引号 表示   例：她低声说道：“你终于来了。”
  ★严禁用{花括号}包裹台词——即梦2.0无法正确识别花括号台词★
  音效环境音→用<尖括号>包裹 例：<雨声和远处雷声持续>
  背景音乐BGM→用（圆括号）包裹 例：（低沉弦乐，节奏缓慢）
· 持续声音标记：镜头切换后若同一声源继续，在描述前方写"<雨声继续>"或"（BGM继续）"
· 台词规范：必须写清语言、音色、语气、语速；有嘴型时写"嘴型同步"
· 一镜到底规则：角色说话场景默认 5-8 秒、一镜到底、正面说话、1-2 句短台词
· 时长节奏：4-5 秒内只安排一个主要动作或一次镜头变化；复杂剧情用 12-15 秒或拆为多镜
· 脸部安全：避免生成写实真人清晰脸部特写（容易被内容审核拦截），面部镜头用中景/侧脸/光影遮挡替代大特写
· 指令一致性：不要同时要求"固定镜头"又写"环绕""推拉摇移"
· 内容不过载：4-5 秒内禁止塞入多个动作或多次镜头切换

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【反渣词库 — 禁止使用的空洞词汇及正确替代】— Seedance 2.0 anti-slop
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

❌ 通用膨胀词（永远不要说）：
  cinematic / epic / stunning / breathtaking / masterpiece / 电影感 / 大片感 / 震撼
  → 直接删除，不替换。这些词不传递任何视觉信息。

❌ 空画质助推器（模型不认识）：
  高质量 / 超清 / 4K / 8K / 极致画质 / 精美 / high quality
  → 替换为具体描述：35mm胶片颗粒 / 浅景深 f/2.8 / 柔和漫射光（禁止写画幅比例如2.39:1）

❌ 抽象情感标签（无法拍摄）：
  温馨 / 压抑 / 暧昧 / 廉价 / 恐怖 / 浪漫 / 悲伤 / 孤独 / 紧张 / 愤怒
  → 必须转译为可见依据：
    「温馨」→ 暖黄灯光+低对比度+人物距离近
    「压抑」→ 低矮天花板+窄画框构图+冷灰色调+人物被阴影吞没
    「紧张」→ 手指在桌面下反复捏搓，指节泛白，呼吸短促
    「孤独」→ 人物居画面1/6位置，四周大面积负空间，冷色环境光

❌ 模糊氛围词（缺乏具体性）：
  有氛围的 / 有感觉的 / 独特的 / 特别的 / atmospheric / moody
  → 必须给出具体描述：雾浓度、光的角度、色彩偏向、空间大小

❌ 否定渣词（Negation Slop — 命名一个缺陷就种下了它）：
  "no blur" / "no artifacts" / "不模糊" / "不抖动" / "无噪点" / "无变形" / "不要出现xxx"
  → 视频模型会将否定语句理解为「强调」——写"不模糊"反而会召唤模糊。
  → 修复规则：删除所有否定描述，改为正向锁定你想要的画面状态。
    "不模糊" → 删除，确保已写了清晰的动作描述即可
    "不抖动" → 删除，如果需要稳定就写"固定镜头"或"Steadicam"
    "不要出现路人" → 删除，改为正向描述"空旷的街道"或"人物独自在画面中"
    "无变形" → 删除，改为正向描述"比例准确的全身像"

❌ 标签沙拉（Tag Salad — 逗号关键词堆砌）：
  "girl, sunset, 8K, cinematic, beautiful light, walking, dress"
  → 这是图模型的提示词习惯，在视频模型中效果差。
  → 修复规则：改写为「一句一元素」的散文式拍摄简报。
    标签沙拉 → "女孩穿着白色连衣裙，独自走在被夕阳染成金色的沙滩上，镜头从中景缓慢推进至中近景"
  → 视频模型需要时序和因果关系，而非并列的关键词列表。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【模型机制理解 — 为什么这些规则有效】— Seedance 2.0 model-mechanics
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

视频生成模型的核心行为原理（知道为什么，比记住规则更重要）：

· 提示词是蓝图，不是照片：
  模型不是逐字渲染你写的每一个词，而是从整个提示词中提取「视觉意图」然后生成。
  → 所以：具体比抽象有效（「逆光剪影」> 「看起来很酷」）
  → 所以：空间关系比形容词有效（「人物距离镜头3米」> 「近一点」）

· 保真度预算有限：
  模型把「注意力」分配到提示词的各部分。描述的主体越多、场景越复杂，
  每个元素的细节就越少。这是零和游戏。
  → 单主体镜头 → 更多细节分配给一个角色
  → 多主体镜头 → 每个角色能分到的细节减少，更容易出畸形
  → 极简场景 + 单一焦点 → 最高保真度

· 位置权重：模型对提示词的不同部分赋予不同的权重。
  开头描述的主体 → 最高权重，最稳定
  中间的动作描述 → 中等权重，有波动
  末尾的风格/画质 → 最低权重，可能被忽略
  → 所以：最重要的视觉元素放在前面，风格装饰放在最后

· 能力地图 — 模型擅长和不擅长的：
  ✅ 擅长：单一清晰主体、明确动作轨迹、强光影对比、物理质感、环境氛围
  ❌ 不擅长：多人精确互动、复杂手部动作、精确文字/符号、细微面部表情变化
  → 利用优势：用光影和构图传达情绪，而不是依赖精细微表情
  → 规避劣势：多人场景尽量全景/远景，避免多人特写；精确动作拆分为简单步骤

· 一致性 > 复杂度：
  一个清晰执行的简单创意，远好于一个混乱执行的复杂创意。
  → 宁可一个镜头只有一个核心意图，不要试图在一镜中塞入多个互不相关的元素

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【增强镜头与运镜词汇】— Seedance 2.0 camera + motion
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

景别体系（按信息密度选择）：
  大远景/远景 / 全景 / 中全景 / 中景 / 中近景 / 近景 / 特写 / 大特写 / 极特写

运镜体系（每种有其情感功能）：
  · 固定：稳定性、客观观察、让表演说话
  · 推轨(慢/中/快)：注意力集中、揭示、压迫或亲密
  · 拉轨(慢/快)：揭示环境、疏离、退场感
  · 横移/横摇：跟随运动、展示空间广度
  · 环绕：围绕主体的权力感或审视感
  · 俯仰摇：垂直空间揭示、从局部到整体
  · 手持微晃：纪实感、紧张、主观视角
  · Steadicam浮游：平滑跟随、沉浸式第一人称
  · Dolly Zoom（希区柯克变焦）：背景拉伸/压缩、心理冲击
  · 升格/慢动作：情绪放大、关键瞬间
  · 降格/快动作：时间压缩、喜剧节奏

机位的情感含义：
  · 眼平：平等、日常
  · 低角度仰拍：权力、威胁、崇敬
  · 高角度俯拍：脆弱、渺小、被审视
  · 顶拍/上帝视角：宿命感、全知
  · 过肩：对话参与感、一方的心理优势
  · POV主观视角：代入角色的所见所感

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【增强光影语言】— Seedance 2.0 lighting
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

光源类型：
  硬光（直射阳光/聚光灯）→ 锐利阴影，戏剧冲突
  软光（阴天/柔光箱/窗纱过滤）→ 柔和过渡，自然感
  逆光 → 剪影、神秘、神圣感
  侧光 → 立体感、对峙、分裂人格
  顶光 → 压迫、审讯、孤独
  底光 → 诡异、恐怖、非自然
  实用光源（台灯/烛光/霓虹/手机屏）→ 真实感、亲密

色温体系：
  暖光 2700K-3500K → 温馨、怀旧、日落、室内
  中性 4000K-5000K → 自然、办公室、白天室内
  冷光 5600K-7500K → 科技、医院、月光、凌晨

光影的时间暗示：
  低角度暖光 + 长阴影 → 黄昏/黄金时刻
  顶光 + 短阴影 → 正午
  冷色漫反射 → 阴天/黎明前
  单一实用光源 + 深黑阴影 → 夜晚室内

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【中文提示词专用词汇与模板】— Seedance 2.0 vocab/zh.md
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

▎压缩模板（短提示词快速构建）：
  @Image1为参考，严格保持[主体]不变；仅加入[动作/光线/镜头变化]。
  镜头：[一个动作]。声音：[音效]。
  → 适用场景：I2V 续拍、微调、单一动作补充

▎时间轴模板（时序化分段描述）：
  【风格】[视觉风格关键词]
  【时间轴】0-3s：[开场状态+第一个动作]；3-6s：[转折动作+镜头变化]；6-10s：[落点状态]
  【声音】[环境声/台词/音乐]
  【参考】[美学参考]
  → 适用场景：5秒以上长镜头、多节拍镜头

▎序列/续拍短语（多镜头连续生成时使用）：
  · "本项目状态以已接受视频为准" — 声明前序镜头已生成，当前镜头从上一镜结尾继续
  · "从上一段真实结尾继续" — 明确续拍起点
  · "不要重演上一段动作" — 防止模型重复前序动作
  · "本段只拍当前任务" — 事件密度防火墙，防止过载
  · "后续剧情暂不出现" — 预留排除，防止模型提前剧透

▎12 条中文专属 Slop Traps（中文提示词高频套话，必须替换）：
  · "电影感" → 删除，用具体镜头/胶片规格替代（如"35mm胶片颗粒"；禁止写画幅比例）
  · "氛围感" → 删除，用具体氛围粒子描述（如"薄雾在脚踝处流动"）
  · "高级感" → 删除，用具体光影/色彩/材质描述
  · "大片感" → 删除，用具体构图/运动描述
  · "质感" → 具体化：什么质感？（金属冷光/皮肤暖泽/布料褶皱垂坠感）
  · "震撼" → 删除，用具体视觉冲击描述（如"广角畸变+低角度仰拍"）
  · "唯美" → 删除，用具体美学描述（如"逆光剪影+暖金色调+浅景深"）
  · "史诗级" → 删除，用具体规模描述（如"大远景+千军万马+尘土飞扬"）
  · "超高清" → 替换为具体技术规格（如"4K ProRes / 35mm胶片颗粒"）
  · "杰作" → 删除，模型不认识评价性词汇
  · "绝美" → 删除，用具体视觉描述替代
  · "酷炫转场" → 替换为具体转场技术（如"match cut 匹配剪辑/whip pan 快速横摇"）

▎中文景别/运镜补充词汇（比基础词汇更自然的中文表达）：
  景别：中近景 / 远景定场镜头 / 四分之三侧脸近景
  运镜：缓慢推镜 / 镜头后拉揭示空间 / 横向稳定跟拍 / 弧形绕摄
  光源：柔和侧逆光 / 暖色实用灯 / 冷色月光轮廓光 / 体积光穿过薄雾

▎对白注意事项：
  · 台词格式：说话者+语气+冒号+中文双引号，即 说话者（语气）说：“台词内容”
    例：她低声说道：“你终于来了。”
  · ★严禁用{花括号}包裹台词——即梦2.0无法识别，必须用 ：“” 格式★
  · 唇形同步：如需要嘴型匹配，在台词后标注"嘴型同步"
  · 方括号音色提示：可用[沙哑][清亮][颤抖]等方括号标注音色特质
    例：他[沙哑地]说道：“别走。”

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【画面描述质量铁律】（你的核心竞争力）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. 动作链完整：入画 → 运动过程 → 落点，轨迹每一步都清晰可辨
2. 光影精确：主光源类型+方向+色温+阴影落点+高光反射面
3. 物理质感：材质触感（粗糙/光滑/潮湿/冰冷）、重量感、步伐声音暗示（鞋底材质+地面+声效）
4. 微表情细节："视线从眉心滑落到嘴唇，喉结滚动一次，下眼睑微颤"
5. 氛围粒子：雾浓度、尘埃飘浮方向、植被运动幅度、水面波纹、雨滴大小
6. ★对白/台词分配（最常被忽略、也最致命的铁律）★：
   剧本中出现的角色对白，必须分配到对应时段镜头的「画面内容」里！
   · 严禁只写画面、吞掉台词——角色说话的镜头，画面内容中必须出现 ：“台词内容”
   · 台词格式：说话者+语气+冒号+中文双引号，例：钱阿龙低声说：“你终于来了。”（禁止用{花括号}）
   · 一镜到底对话场景：5-8秒、1-2句短台词、注明"嘴型同步"
   · 长对话拆为多镜时，按时间把台词分配到各镜，不要把整段对话只塞进一镜、更不能全部丢弃
   · 仅当该时段剧本确实没有对白（纯动作/环境镜头）时，才允许不写台词
   · 台词必须忠实于剧本原文，不可自行编造或改写台词内容

❌ 绝对禁止：
  · "他感到悲伤/她意识到危险" → 转译为：低头、垂肩、咬唇、瞳孔收缩、后退半步
  · 背后/过肩镜头描写正面五官（透视屏蔽）
  · 干瘪的一句话 → 每个镜头画面内容必须 ≥80 字
  · 任何上述反渣词库中的词汇
  · ★只写画面不分配台词——剧本有对白但镜头画面内容里没有 ：“台词”★
  · ★用{花括号}包裹台词——即梦2.0无法识别，必须用 ：“” 格式★

✅ 参考级密度（你要达到的标准 — 注意台词：“”/音效<>/BGM（）如何嵌入）：
  「瘴气流动的黑水林深处，月光艰难穿透雾气在地面投下斑驳的苍白光斑。
   钱阿龙身穿正红新郎服，步履僵硬地从画面深处走来，每一步脚跟先砸在地上，
   重心前倾，膝盖不打弯，像被无形丝线牵引的木偶。他走到一棵枝叶盘虬的
   古榕树前停住，低声说：“我不该来的。”<远处传来犬吠声>，（低沉弦乐渐起）。
   肩膀有节奏地轻微耸动，喉间发出一阵混合着笑声和哭泣的诡异呜咽。
   推轨镜头缓慢向前，灌木枝条轻轻擦过镜头边缘，形成一层虚化的绿色前景。」""",
        llm=director_llm,
        verbose=False,
        allow_delegation=False
    )

    # ═══════════════════════════════════════════════════════════════════
    # Agent 1: 分镜质检（v4.0 — 新增 Seedance 规则专项检查）
    # ═══════════════════════════════════════════════════════════════════
    qa_reviewer_agent = Agent(
        role='分镜质检',
        goal='抽查Director的JSON输出，修正创意质量问题，保证画面描述密度达标，验证Seedance规则合规',
        backstory="""你是分镜质量审查员。Director已输出结构化JSON，你需要做系统化审查。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【第一轮：语义级抽查】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. 画面描述密度：是否有镜头画面内容 <60字？→ 补充动作/光影/质感细节
2. 抽象心理转译：是否有"他感到""她意识到"等不可拍摄内容？→ 转译为可见肢体动作
3. 透视屏蔽：背后/过肩镜头是否违规描写了正面五官？→ 修正视角
4. 场景一致性：镜头引用的场景名是否在场景定义中存在？→ 修正或补定义
5. felt_intent 一致性：相邻镜头是否有完全相同的 felt_intent？→ 每个镜头的意图必须独立、独特

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【第二轮：Seedance 规则合规审查】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

A. 音频符号正确性：台词是否用 ：“” （冒号+中文双引号）？音效用<>？BGM用（）？
   → 台词若出现{花括号}包裹，必须改写为 说话者+语气说：“台词” 格式（即梦2.0不识别花括号）

B. 指令冲突检测：「固定镜头」和「推/拉/摇/移/环绕」是否同时出现？→ 冲突则保留其一

C. 内容过载检测（事件密度防火墙）：
   · 4-5秒镜头内是否有 ≥2 个独立动作 + ≥2 次镜头切换？→ 拆分或延长至12-15秒
   · 是否有单一镜头塞入完整对话+打斗+场景转换？→ 拆分为多镜
   · 事件密度公式：独立事件数 / 秒数 > 0.5 → 过载，需要拆分

D. 持续声音标记：同一场景连续镜头中，若前镜有环境声/BGM，后镜是否标记了"<xx继续>"或"（BGM继续）"？

E. 脸部安全：是否有"写实真人清晰面部特写""毛孔级面部细节"等风险描述？→ 降级为"中景侧脸""光影遮挡面部"

F. 系统化反渣检查（逐个镜头扫描以下6类词汇）：
   · 通用膨胀词：cinematic / epic / stunning / breathtaking / masterpiece / 电影感 / 大片感 / 震撼 → 删除，不替换
   · 空画质助推器：高质量 / 超清 / 4K / 8K / 极致画质 / 精美 / high quality → 替换为具体技术描述
   · 抽象情感标签：温馨 / 压抑 / 暧昧 / 廉价 / 恐怖 / 浪漫 / 悲伤 / 孤独 / 紧张 / 愤怒 → 转为可见视觉依据
   · 模糊氛围词：有氛围的 / 有感觉的 / 独特的 / atmospheric / moody → 给出具体视觉描述
   · 否定渣词：no blur / no artifacts / 不模糊 / 不抖动 / 无噪点 / 不要出现xxx → 删除否定语句，改为正向锁定
   · 标签沙拉：逗号关键词堆砌（"girl, sunset, 8K, cinematic"）→ 改写为散文式拍摄简报
   · 发现上述任何词汇 → 必须修正，不能保留

G. felt_intent 质量审查：
   · felt_intent 是否为抽象情感词（"悲伤""紧张"）？→ 改为具体感知方向
   · felt_intent 是否太泛（"观众被吸引"）？→ 给出具体的情感/感知锚点
   · felt_intent 是否与景别/机位/运镜选择逻辑一致？→ 不一致则修正

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【第三轮：重拍决策协议 — 精细化质量控制】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

不是简单的"通过/不通过"二选一。你需要判断每个镜头的质量等级并做出相应决策：

· 质量分级：
  A级（≥90%正确）：保留原输出，不做修改
  B级（80-89%正确）：微调修正——补充密度不足的画面内容、修正个别反渣词
  C级（60-79%正确）：定向重写——保留该镜的 felt_intent + 场景名 + 出场角色，
      重写画面内容、景别、机位、运镜、构图
  D级（<60%正确）：完全重写该镜头——只保留镜头号和场景名，其他全部重来

· 80%阈值判断标准：
  达到80%标准 = felt_intent正确 + 场景名正确 + 出场角色正确 + 主要内容方向对
  不达标 = 核心要素错配（如意图为「压迫感」却用了眼平中景+柔光）

· 单变量重拍原则：
  修正一个镜头时，只改动一个维度（画面内容 / 景别 / 运镜 / 构图），
  其他维度保持不变。这样可以精确追踪哪个改动解决了问题。
  例：画面内容密度不足 → 只扩充画面内容，不改变景别和运镜。

· 尝试预算：
  每个镜头最多重写2次。如果2次修正后仍达不到B级（80%），标记该镜为
  「⚠️ 需人工审核」并保留最好的那次输出。不要无限循环修正。

· 故障分类速查（常见问题 → 对应动作）：
  画面内容 <60字 → 扩充画面内容（不改镜头参数）| A级→微调
  反渣词出现 → 删除/替换（不改镜头参数）| A/B级→微调
  意图与镜头参数矛盾 → 修正镜头参数以匹配意图 | B/C级→定向重写
  场景名引用错误 → 修正场景名（不改其他）| B级→微调
  多动作过载 → 拆分镜头或延长时长 | C级→定向重写
  核心要素全错 → 完全重写 | D级→完全重写

修正后直接输出完整 JSON，不要解释改了什么。纯 JSON，无 Markdown。""",
        llm=qa_llm or director_llm,
        verbose=False,
        allow_delegation=False
    )

    return {
        'director': director_agent,
        'qa_reviewer': qa_reviewer_agent
    }


# ═════════════════════════════════════════════════════════════════════════════
# Task 工厂 — v4.0
# ═════════════════════════════════════════════════════════════════════════════

def create_tasks(agents: dict) -> dict:
    """v4.0：2 个 Task（Director + QA，含 Seedance 2.0 规则约束）"""

    # ── Task 0: 分镜导演（结构化 JSON 输出）──
    task_director = Task(
        description="""请仔细阅读以下剧本，分析后输出结构化分镜 JSON。

{script}

{char_injection}
{duration_guide}

用户视觉基调参考（可为空）：{style}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【导演阅读前置步骤 — 输出 JSON 前必须先完成】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

在推导每个镜头的 felt_intent 之前，先对剧本中的每个场景做一次「导演阅读 5 问法」：
1. 功能：这场戏的叙事功能是什么？
2. 转折：场景内部有无情感/权力/信息转变？转折点在哪？
3. POV：观众站在谁的视角？
4. 权力：谁在支配局面？权力是否翻转？
5. 潜台词：角色嘴上说 vs 真正想表达的是什么？→ 这就是 felt_intent 的来源

阅读完成后，先确定本项目的「导演声音」（6 选 1），再逐镜推导 felt_intent 和设置。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【你唯一的输出格式 — 严格 JSON】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

{
  "全局氛围画质": "完整的【氛围与画质】描述文本（≥120字）。必须包含：风格核心关键词、视觉基调、色彩与影调、镜头/胶片模拟、质感描述、风格参考锚点（影视作品/美学流派）。★禁止写画幅/银幕比例（如变形宽银幕、2.39:1、IMAX画幅）——这是AI视频生成，不是院线电影，画幅由生成平台决定★。例如：'风格核心：赛博朋克、霓虹暗黑。视觉基调：夜景为主，人工光源主导，高反差光比。色彩影调：深紫+冷蓝高反差，霓虹品红点缀，胶片颗粒质感。镜头：浅景深，湿润街面反光。参考美学：《银翼杀手2049》...'",
  "director_voice": "从6种导演声音中选择1种作为本项目主导声音（观察自然主义者/构图古典主义者/动感本能派/表达风格主义者/亲密极简主义者/图形形式主义者）。选定后所有镜头的风格倾向应向此声音靠拢。",
  "场景定义": {
    "场景名": "时间：xx | 地点：xx | 主光源：方向+色温 | 氛围粒子：xx | 声音环境：xx（50-80字）"
  },
  "分镜列表": [
    {
      "镜头号": 1,
      "场景名": "场景定义中已定义的场景名",
      "felt_intent": "观众应感受到的具体内容（10-20字）。不是抽象情感词，而是具体的感知方向。如：'权力关系逆转的瞬间沉默' / '被窥视的不安逐渐累积' / '漫长等待后的释然'",
      "时长秒": 5,
      "景别": "全景",
      "机位": "平视低角度，贴近地面",
      "构图": "人物居中，前景灌木形成引导线，天空占1/3",
      "运镜": "慢速向前推轨",
      "画面内容": "该镜头的完整画面描述（≥80字）。必须包含：角色动作轨迹（入画→过程→落点）、光影效果（光源类型+方向+色温+阴影位置）、物理质感（材质+声音暗示）、氛围细节（雾/尘埃/植被运动）、★角色对白（剧本中该时段的台词必须用 ：“台词” 格式写入画面内容，注明说话者+语气；禁止用{花括号}；无对白的纯动作镜头才可省略）★。角色名直接写原名如'钱阿龙'，无需加@符号。",
      "出场角色": ["钱阿龙"]
    }
  ]
}

字段说明：
  · 全局氛围画质：一个完整文本块，不是键值对。输出给用户当作整个项目的视觉设定参考。
  · director_voice：★ 项目级导演声音 ★ 从6种导演声音中选择1种。这决定所有镜头的风格倾向。
    选定后，景别/机位/运镜/灯光的选择都应受此声音影响。80%以上镜头遵循主导声音。
  · 场景定义：对本切块中出现的每个场景给出环境描述。场景名用简短中文名（如"黑水林""阿龙婚房"）。
  · 镜头号：从1开始的整数。
  · felt_intent：★ 新增核心字段 ★ 这一镜观众应该感受到什么（10-20字）。不是"悲伤""紧张"抽象词，而是具体感知方向。
    例："权力关系悄然逆转""被窥视的不安逐渐累积""漫长对峙中率先动摇的瞬间""独自一人时的脆弱暴露"
    felt_intent 驱动你的所有选择：景别/机位/运镜/光影/节奏都必须服务于这个意图。
  · 时长秒：该镜建议持续秒数。单动作/单切镜=4-5s；对话文戏=5-8s；武戏打斗=2-3s；过渡衔接=2-3s；复杂剧情=12-15s 或拆多镜。绝不在4-5秒内塞≥2个独立动作。
  · 景别：大远景/远景/全景/中全景/中景/中近景/近景/特写/大特写/极特写。避免写实真人面部大特写（改中景/侧脸）。
  · 机位：拍摄高度+角度+距离+情感含义。如"低角度仰拍，制造压迫感""高机位俯拍，人物显渺小""眼平机位，日常感"。
  · 构图：人物在画面中的位置关系、前景/背景层次、引导线、三分法/黄金分割、负空间比例。
  · 运镜：运动方式+速度+情感功能。如"慢速推轨，逐渐逼近关键信息""手持微晃，纪实紧张感""Steadicam浮游，沉浸式跟随"。禁止「固定镜头」+「推拉摇移环绕」同时出现。
  · 画面内容：★ 核心创意输出 ★ 角色名直接写原名（如"钱阿龙"），代码会自动加@前缀。禁止只写"他走进来"这种干瘪描述！
    ★★台词分配铁律★★：剧本中的角色对白必须分配到对应时段镜头的「画面内容」里，用 冒号+中文双引号 表示台词并注明说话者+语气。
    严禁只写画面动作而吞掉剧本对白——每个有台词的镜头，画面内容中都必须出现 ：“台词内容”。
    长对话段落拆为多镜时，按时间把台词分配到各镜，不要把整段对话只塞进一镜或全部丢弃。
    画面内容中音频符号约定：台词用 ：“”（严禁{花括号}，即梦2.0不识别）、音效用<>、BGM用（）。
    例：钱阿龙压低声音说：“别出声。”<远处传来犬吠声>，（低沉钢琴渐起）
    禁止使用反渣词库中的任何词汇（电影感/大片感/高质量/温馨/压抑/恐怖等）。
  · 出场角色：该镜头中出现的角色名列表，按出场顺序。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【按上方「剧本体量分析」作为参考，自主判断】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  上方代码计算的范围仅供参考，你需要结合剧本的实际节奏和戏剧密度自主判断合理分镜数。
  
  分镜数量判断原则：
  · 宁可少出几个高质量镜头，也不要出一堆薄弱的凑数镜头
  · 每次切镜必须有独立的视觉价值（新的景别/机位/运镜/情绪节奏变化）
  · 短剧本/动作密集段落 → 少镜+长镜（每镜画面内容要丰富可见）
  · 长对话段落 → 可适当多镜但要避免拆分连贯对话节奏
  · 对话段落参考：3-5镜/段（一镜到底对话5-8s） | 武戏段落：5-8镜/段（2-3s/镜） | 过渡段落：2-3镜/段（2-3s/镜）
  
  每镜严格遵守：4-5s内=一个主动作/一次切镜，复杂剧情=12-15s或拆多镜

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【输出铁律】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. 纯 JSON，无任何 Markdown 标记（不要 ```json 包裹）
2. 无任何解释/分析/元评论文本
3. 画面内容每个 ≥80 字，核心镜头 ≥120 字
4. 每个镜头的 felt_intent 必须独特、具体、非重复（相邻两镜意图不得完全相同）
5. 不得使用反渣词库中的任何词汇（cinematic/epic/电影感/大片感/高质量/温馨/压抑/恐怖/暧昧/浪漫/悲伤等）
6. JSON 必须可被 Python json.loads() 直接解析
7. ★★★台词分配强制要求★★★：剧本中所有角色对白必须分配到对应时段镜头的「画面内容」中（用 ：“台词” 格式，注明说话者+语气，严禁{花括号}）。严禁只输出画面描述而吞掉剧本台词——这是最严重的错误。逐镜检查：该时段剧本有对白吗？有→画面内容里必须有 ：“台词”；无→才可只写画面。""",
        expected_output="""一个严格的 JSON 对象，包含「全局氛围画质」「director_voice」「场景定义」「分镜列表」四个字段。
分镜列表中每个元素含10个字段（镜头号/场景名/felt_intent/时长秒/景别/机位/构图/运镜/画面内容/出场角色）。
director_voice 标注项目级导演声音选择，felt_intent 驱动该镜所有创意选择，画面内容描述达到专业影视级密度。
纯JSON，无Markdown，无解释文字，无反渣词。""",
        agent=agents['director']
    )

    # ── Task 1: 分镜质检（v4.0 新增 Seedance 规则审查）──
    task_qa_review = Task(
        description="""你是分镜质检。审查Director的结构化JSON输出，修正后输出。

你可以看到Director的完整JSON输出。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【审查项目】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

A. 画面描述密度：
   - 是否有镜头「画面内容」<60字？→ 补充动作轨迹、光影细节、物理质感
   - 是否有镜头只写了动作没有氛围？→ 补充雾/尘埃/光影/声音等环境细节

B. 抽象心理转译：
   - 是否有「他感到紧张」「她意识到危险」等不可拍摄内容？→ 转译为肢体动作
   - 「紧张」→「手指在桌面下反复捏搓，指节泛白」
   - 「愤怒」→「下颌咬紧，太阳穴青筋微凸，手中的杯子被攥得发出细响」

C. 透视屏蔽：
   - 背后/过肩镜头是否违规描写了正面五官？→ 修正为可见的身体部位（后脑/后背/肩膀/手臂）

D. 场景一致性：
   - 分镜引用的场景名是否在「场景定义」中存在？→ 不存在则补充或修正

E. felt_intent 审查：
   - 是否有镜头缺少 felt_intent？→ 补充
   - felt_intent 是否为抽象情感词（"悲伤""紧张"）？→ 改为具体感知方向，如"权力关系悄然逆转"
   - 相邻两镜 felt_intent 是否完全相同？→ 每镜意图必须独立独特
   - felt_intent 与景别/机位/运镜选择是否逻辑一致？→ 不一致则修正

F. 音频符号正确性（即梦2.0兼容）：
   - 台词是否用 ：“” （冒号+中文双引号）？→ 若发现{花括号}包裹的台词，必须改写为 说话者+语气说：“台词”
   - 环境音/音效是否用<>？→ 修正为<尖括号>
   - BGM是否用（）？→ 修正为（圆括号）
   - 没有音效/台词的镜头不要强行添加符号

G. 指令冲突检测：
   - 「固定镜头」是否与「推/拉/摇/移/环绕」同时出现在同一镜头？→ 冲突则删除其一
   - 「无声」是否与「背景音乐」同时出现？→ 冲突则保留其一

H. 事件密度防火墙：
   - 4-5 秒镜头内是否有 ≥2 个独立动作 + ≥2 次镜头切换？→ 拆分镜头或延长至12-15秒
   - 是否有单一镜头塞入完整对话+打斗+场景转换？→ 拆分为多镜
   - 独立事件数 / 秒数 > 0.5 → 过载，必须拆分

I. 持续声音标记：
   - 同一场景连续镜头中，若前镜有"<雨声>""（BGM）"，后镜画面描述起始处是否写"<雨声继续>""（BGM继续）"？

J. 脸部安全：
   - 是否出现"面部大特写""写实真人清晰脸部""毛孔级面部细节"？→ 降级

K. 系统化反渣检查（最重要！逐镜扫描）：
   必须检测并修正以下六类词汇：
   · 膨胀词：cinematic / epic / stunning / breathtaking / masterpiece / 电影感 / 大片感 / 震撼 → 直接删除
   · 空助推器：高质量 / 超清 / 4K / 8K / 极致画质 / 精美 / high quality → 替换为具体技术描述
   · 抽象标签：温馨 / 压抑 / 暧昧 / 廉价 / 恐怖 / 浪漫 / 悲伤 / 孤独 / 紧张 / 愤怒 → 转为可见依据
   · 模糊词：有氛围的 / 有感觉的 / 独特的 / 特别的 / atmospheric / moody → 给出具体视觉描述
   · 否定渣词：no blur / no artifacts / 不模糊 / 不抖动 / 无噪点 / 不要出现xxx → 删除否定语句，改为正向锁定
   · 标签沙拉：逗号关键词堆砌（"girl, sunset, 8K, cinematic"）→ 改写为散文式拍摄简报
   · 画幅比例词：变形宽银幕 / 2.39:1 / 2.35:1 / IMAX画幅 / 宽银幕比例 → 直接删除（AI视频生成的画幅由平台决定，写了也无效，还会污染提示词）。「全局氛围画质」中若出现同样删除
   发现任何上述词汇，必须修正！不能保留！

L. ★台词完整性审查（最重要！防止"只有画面没有对话"的致命错误）★：
   你能看到原始剧本和Director的JSON输出。逐镜对照检查：
   1. 该镜头所覆盖的剧本时段中，是否有角色对白？
      → 有对白：镜头「画面内容」中是否包含了对应的 ：“台词内容”？
        · 如果没有 → ★必须补回★：在该镜头画面内容中加入 说话者+语气说：“台词内容”
        · 台词必须忠实剧本原文，不可改写
      → 无对白（纯动作/环境镜头）：允许画面内容中不出现台词
   2. 是否有镜头只写了画面动作，却吞掉了剧本中该时段的对白？→ 必须补回台词
   3. 长对话段落被拆为多镜时，台词是否只出现在其中一镜、其余镜丢失？→ 按时间把台词分配到对应各镜
   4. 台词是否注明了说话者+语气？→ 补充为"说话者+语气说：“台词”"
   5. 台词格式是否正确使用了 ：“” （冒号+中文双引号）？→ 若发现{花括号}/()/<>包裹的台词，一律改写为 ：“台词” 格式（即梦2.0只识别此格式）
   这是最高优先级审查项——剧本有台词却不出现在分镜中，是分镜系统最严重的功能性缺陷。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【输出格式】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

输出修正后的完整 JSON（结构与Director输出完全相同，含 felt_intent 字段）。
纯 JSON，无 Markdown 标记，无解释文字。
如果无需修正，原样输出。""",
        expected_output="""修正后的完整 JSON 对象，与Director输出结构完全一致。
内容已修正至专业影视级质量标准，Seedance 2.0 规则全部合规。
纯JSON，无Markdown，无解释。""",
        agent=agents['qa_reviewer'],
        context=[task_director]
    )

    return {
        'task_director': task_director,
        'task_qa_review': task_qa_review
    }


# ═════════════════════════════════════════════════════════════════════════════
# 代码组装引擎 — v4.1 核心：结构化 JSON → Seedance 2.0 四段式提示词（纯文本无占位符）
# ═════════════════════════════════════════════════════════════════════════════

def _format_timecode(total_seconds: float) -> str:
    """将累计秒数格式化为 MM:SS 时间码字符串。"""
    m = int(total_seconds // 60)
    s = int(total_seconds % 60)
    return f"{m:02d}:{s:02d}"


def _inject_references(content: str, scene_name: str, characters: list) -> str:
    """
    在画面内容中自动插入 @角色名 引用。
    保护音频标记内的文本不被 @ 注入破坏：
    台词 ：“…”（即梦2.0格式）、旧版{…}、音效<…>、BGM（…）。

    规则：
    - 每个角色名的第一次出现（且不在台词/音效/BGM标记内）替换为 @角色名
    - 同一角色在同一镜头内多次出现时，仅第一次加@
    """
    result = content

    for char in characters:
        if not char or char not in result:
            continue
        # 保护音频标记内的文本：先用占位符替换 “”/{}/<>/（）内容，注入后再恢复
        audio_blocks = []
        def _mask_audio(m):
            audio_blocks.append(m.group(0))
            return f"\x00AUDIO{len(audio_blocks)-1}\x00"
        masked = re.sub(r'“[^”]*”|\{[^}]*\}|<[^>]*>|（[^）]*）', _mask_audio, result)
        # 在 mask 后的文本中做 @注入
        if char in masked:
            masked = masked.replace(char, f"@{char}", 1)
        # 恢复音频块
        for i, block in enumerate(audio_blocks):
            masked = masked.replace(f"\x00AUDIO{i}\x00", block)
        result = masked

    return result


def _extract_character_brief(chars: list, content: str) -> dict:
    """
    为 Section 1「主体与特征锚定」提取角色的 2-3 个关键视觉特征。
    从画面内容中提取首次出现该角色时的前后文字作为简要特征描述。
    返回 {角色名: "2-3个关键特征"} 的映射。
    """
    briefs = {}
    for char in chars:
        if not char:
            continue
        idx = content.find(char)
        if idx >= 0:
            # 取角色名后 30 字作为特征描述上下文
            ctx = content[idx + len(char):idx + len(char) + 30].strip()
            # 找最近的第一个自然断点（6 字以上才有意义）
            cut = ctx
            best_pos = len(ctx)
            for sep in ['。', '，', '；', '、', '\n']:
                pos = ctx.find(sep)
                if 6 < pos < best_pos:
                    best_pos = pos
            if best_pos < len(ctx):
                cut = ctx[:best_pos]
            cut = cut.replace('@', '').strip()
            if len(cut) >= 2:
                # 最多保留 30 字
                briefs[char] = cut[:30].strip()
    return briefs


def _extract_scene_brief(scene_desc: str) -> str:
    """
    从完整场景描述中提取简要标识（时间 + 地点），用于 Section 1。
    避免硬截断导致残破文本。
    """
    if not scene_desc:
        return ""
    # 从 | 分隔的场景定义中提取 时间 和 地点 字段
    parts = {}
    for segment in scene_desc.replace("｜", "|").split("|"):
        segment = segment.strip()
        for sep_char in ("：", ":"):
            if sep_char in segment:
                key, val = segment.split(sep_char, 1)
                key = key.strip()
                if key in ("时间", "地点"):
                    parts[key] = val.strip()[:20]
                break
    if "地点" in parts:
        time_part = f"{parts['时间']}，" if "时间" in parts else ""
        return f"{time_part}{parts['地点']}"
    # 回退：安全截断
    return scene_desc.split("|")[0].strip()[:40]


def _resolve_multimodal_references(
    content: str,
    reference_images: list = None,
    reference_videos: list = None,
    reference_audios: list = None
) -> str:
    """
    [预留扩展接口 — Seedance 2.0 多模态引用语法]
    
    当前（v4.3）：纯文本 T2V 工作流，此函数直接返回原文。
    未来接入 I2V / V2V / FLF2V 等模态时，在此实现：
    
      @Image1-9 语法解析：
        - 扫描 content 中的 @Image1 ~ @Image9
        - 将占位符替换为 reference_images 中对应的媒体 URI / 文件路径
        - 每种引用按角色分离：身份/环境/运动/摄像机节奏/音频节拍/风格/端点
    
      @Video1-3 语法解析：
        - 同理，替换为 reference_videos 中的视频片段引用
    
      @Audio1-3 语法解析：
        - 同理，替换为 reference_audios 中的音频引用
    
    参考：Seedance 2.0 reference-workflow.md / reference-transfer-contract.md
    
    参数:
      content: 原始画面内容文本
      reference_images: [预留] 图片引用列表，每项 {uri, role, label}
      reference_videos: [预留] 视频引用列表
      reference_audios: [预留] 音频引用列表
    
    返回:
      注入多模态引用后的内容文本（当前直接返回原文本）
    """
    # TODO: 当项目接入多模态工作流时实现
    # 当前阶段：纯文本 T2V，所有引用语法暂不解析
    return content


def assemble_seedance_prompt(shot_data: dict, time_offset_seconds: float = 0.0) -> tuple:
    """
    v4.1 核心组装函数：按 Seedance 2.0 四段式格式生成提示词。

    四段式结构（纯文本工作流，无 <主体N> 占位符）：
      1. 主体与特征锚定 — 角色名 + 2-3 个视觉特征，场景名 + 时间/地点
      2. 参考关系和子任务判断 — 任务类型 + 故事概要（自然语言）
      3. 动态描述 — 时序分段的动作/镜头/台词：“”/音效<>/BGM（）
      4. 静态描述 — 光线/色彩/画质/风格（不重复第3段已有内容）

    v4.3 新增：felt_intent 意图声明 — 在各段之前声明本镜的导演意图，驱动所有视觉选择。

    参数:
      shot_data: Director/QA 输出的 JSON dict
      time_offset_seconds: 跨切块时间码偏移

    返回:
      (shot_list, total_seconds, global_atmosphere)
    """
    shot_list = []
    accumulated = time_offset_seconds
    global_atmosphere = shot_data.get("全局氛围画质", "") if isinstance(shot_data, dict) else ""
    # v4.4: 提取导演声音，追加到全局氛围输出中
    director_voice = shot_data.get("director_voice", "") if isinstance(shot_data, dict) else ""
    if director_voice:
        global_atmosphere = f"[导演声音] {director_voice}\n{global_atmosphere}" if global_atmosphere else f"[导演声音] {director_voice}"
    scene_defs = shot_data.get("场景定义", {}) if isinstance(shot_data, dict) else {}
    shots = shot_data.get("分镜列表", []) if isinstance(shot_data, dict) else []

    # 防御：确保 shots 是列表（LLM 可能返回字符串）
    if isinstance(shots, str):
        logger.warning(f"assemble_seedance_prompt: 分镜列表是字符串而非列表，尝试JSON解析")
        try:
            shots = json.loads(shots)
        except (json.JSONDecodeError, TypeError):
            logger.error(f"assemble_seedance_prompt: 分镜列表字符串无法解析为JSON: {shots[:200]}")
            shots = []
    if not isinstance(shots, list):
        logger.warning(f"assemble_seedance_prompt: 分镜列表类型异常({type(shots).__name__})，返回空列表")
        shots = []

    if not shots:
        return [], time_offset_seconds, global_atmosphere

    # 预计算全局氛围摘要（用于 Section 4 静态描述注入）
    global_atmo_summary = ""
    if global_atmosphere:
        atmo = global_atmosphere.strip()
        # 取前 200 字作为风格/画质摘要
        if len(atmo) > 200:
            cut_pos = atmo[:200].rfind('。')
            if cut_pos > 50:
                global_atmo_summary = atmo[:cut_pos + 1]
            else:
                global_atmo_summary = atmo[:200] + '…'
        else:
            global_atmo_summary = atmo

    for shot in shots:
        # 防御：确保每个 shot 是字典（LLM 可能返回字符串元素）
        if not isinstance(shot, dict):
            logger.warning(f"assemble_seedance_prompt: 跳过非字典镜头: type={type(shot).__name__}, value={str(shot)[:100]}")
            continue
        # ── 基本字段提取 ──
        shot_num = shot.get("镜头号", len(shot_list) + 1)
        scene_name = str(shot.get("场景名", "")).strip()
        felt_intent = str(shot.get("felt_intent", "")).strip()
        duration = max(float(shot.get("时长秒", 4)), 1.0)
        shot_type = str(shot.get("景别", "")).strip()
        camera_pos = str(shot.get("机位", "")).strip()
        composition = str(shot.get("构图", "")).strip()
        camera_move = str(shot.get("运镜", "")).strip()
        content = str(shot.get("画面内容", "")).strip()
        characters = shot.get("出场角色", [])
        if isinstance(characters, str):
            characters = [c.strip() for c in characters.replace("、", ",").split(",") if c.strip()]

        # ── 时间码计算 ──
        start_sec = accumulated
        end_sec = accumulated + duration
        timecode = f"{_format_timecode(start_sec)}~{_format_timecode(end_sec)}"
        accumulated = end_sec

        # ── 景别机位运镜摘要列（不变）──
        camera_parts = [p for p in [shot_type, camera_pos, camera_move] if p]
        camera_summary = "，".join(camera_parts)

        # ── 场景描述提取 ──
        scene_desc = ""
        if isinstance(scene_defs, dict) and scene_name in scene_defs:
            scene_desc = str(scene_defs[scene_name]).strip()

        # ── 角色特征简要提取 ──
        char_briefs = _extract_character_brief(characters, content)

        # ════════════════════════════════════════
        # Section 1: 主体与特征锚定（纯文本，无占位符）
        # ════════════════════════════════════════
        # 格式：缩进列表，角色写关键特征，场景写时间+地点
        # [扩展预留] 未来多模态阶段可在此注入 @Image/@Video 引用标签
        #   例：· 钱阿龙：@Image1 正红新郎服，步履僵硬 @Image2
        section1_lines = []
        for char in characters[:5]:
            brief = char_briefs.get(char, "")
            if brief:
                section1_lines.append(f"  · {char}：{brief}")
            else:
                section1_lines.append(f"  · {char}")
        if scene_name:
            scene_brief = _extract_scene_brief(scene_desc)
            section1_lines.append(f"  · {scene_name}：{scene_brief}" if scene_brief else f"  · {scene_name}")

        # ════════════════════════════════════════
        # Section 2: 参考关系和子任务判断（自然语言，简洁概要）
        # ════════════════════════════════════════
        char_names = "、".join(characters[:5]) if characters else "角色"
        section2 = f"任务类型：参考。根据剧本设定，生成 {char_names} 在 {scene_name or '场景'} 中的视频。"

        # ════════════════════════════════════════
        # Section 3: 动态描述
        # ════════════════════════════════════════
        # 注入 @引用
        content_with_refs = _inject_references(content, scene_name, characters)

        # 构建镜头参数行
        camera_line_parts = []
        if shot_type:
            camera_line_parts.append(f"景别：{shot_type}")
        if camera_pos:
            camera_line_parts.append(f"机位：{camera_pos}")
        if composition:
            camera_line_parts.append(f"构图：{composition}")
        if camera_move:
            camera_line_parts.append(f"运镜：{camera_move}")
        camera_line = "，".join(camera_line_parts)

        # 组装 Section 3：镜头参数行 + 画面内容
        section3 = f"@{scene_name}\n\n{camera_line}。\n{content_with_refs}"

        # ════════════════════════════════════════
        # Section 4: 静态描述
        # ════════════════════════════════════════
        section4_parts = []
        # 场景环境信息（光线/色彩部分）
        if scene_desc:
            section4_parts.append(f"场景环境：{scene_desc}")
        # 全局画质/风格（取摘要，不重复第3段已有的动作描述）
        if global_atmo_summary:
            section4_parts.append(f"画质与风格参考：{global_atmo_summary}")
        # 如果都为空，给出默认提示
        if not section4_parts:
            section4_parts.append("（与第3段动态描述保持一致，无额外静态元素需要补充）")

        section4 = "\n".join(section4_parts)

        # ════════════════════════════════════════
        # 组装最终提示词
        # ════════════════════════════════════════
        header_line = f"═══════════════════════════════════════\n分镜{shot_num}：{timecode}（{duration}秒）\n═══════════════════════════════════════"

        # 意图编译（v4.4：felt_intent 从声明升级为编译）
        # 意图不再只作为标签声明，而是编译为具体的可见选择清单
        intent_declaration = ""
        if felt_intent:
            compile_parts = []
            if shot_type:
                compile_parts.append(f"景别={shot_type}")
            if camera_pos:
                compile_parts.append(f"机位={camera_pos}")
            if camera_move:
                compile_parts.append(f"运镜={camera_move}")
            if composition:
                compile_parts.append(f"构图={composition}")
            compile_line = " | ".join(compile_parts) if compile_parts else "见下方动态描述"
            intent_declaration = (
                f"\n【导演意图】{felt_intent}\n"
                f"  → 编译为可见选择：{compile_line}\n"
                f"  → 意图已编译为下方动态/静态描述中的具体灯光/表演/声音/空间条款\n"
            )

        final_prompt = (
            f"{header_line}{intent_declaration}\n"
            f"1. 主体与特征锚定\n"
            + "\n".join(section1_lines) + "\n\n"
            f"2. 参考关系和子任务判断\n"
            f"{section2}\n\n"
            f"3. 动态描述\n"
            f"{section3}\n\n"
            f"4. 静态描述\n"
            f"{section4}"
        )

        shot_list.append({
            "镜头号": str(shot_num),
            "时间码": timecode,
            "景别机位运镜": camera_summary,
            "终极Seedance提示词": final_prompt
        })

    total_seconds = accumulated - time_offset_seconds
    return shot_list, total_seconds, global_atmosphere


# ═════════════════════════════════════════════════════════════════════════════
# Crew 工厂 — v4.0（2 Agent）
# ═════════════════════════════════════════════════════════════════════════════

def create_crew(agents: dict, tasks: dict) -> Crew:
    """v4.0：创建 Director + QA 双 Agent 顺序 Crew"""
    return Crew(
        agents=[agents['director'], agents['qa_reviewer']],
        tasks=[tasks['task_director'], tasks['task_qa_review']],
        process=Process.sequential,
        verbose=True
    )


def create_crew_director_only(agents: dict, tasks: dict) -> Crew:
    """创建仅含 Director 的 Crew（并行模式第一步）。"""
    return Crew(
        agents=[agents['director']],
        tasks=[tasks['task_director']],
        process=Process.sequential,
        verbose=True
    )


def create_crew_qa_only(agents: dict, tasks: dict) -> Crew:
    """创建仅含 QA 的 Crew（并行模式 QA 单独跑）。"""
    return Crew(
        agents=[agents['qa_reviewer']],
        tasks=[tasks['task_qa_review']],
        process=Process.sequential,
        verbose=True
    )


# ═════════════════════════════════════════════════════════════════════════════
# JSON 解析 — v4.0（适配新 Schema）
# ═════════════════════════════════════════════════════════════════════════════

def parse_structured_json(raw_text: str) -> dict:
    """
    v4.0：从 LLM 输出中提取结构化分镜 JSON（全局氛围画质 + 场景定义 + 分镜列表）。
    返回 dict 或 None。
    """
    import json as _json
    
    if not raw_text or not raw_text.strip():
        return None
    
    text = raw_text.strip()
    
    # 尝试1：直接解析整个文本
    try:
        data = _json.loads(text)
        if isinstance(data, dict) and "分镜列表" in data:
            return data
    except _json.JSONDecodeError:
        pass
    
    # 尝试2：提取 JSON 块（可能被 markdown 包裹）
    json_start = text.find('{')
    json_end = text.rfind('}')
    if json_start != -1 and json_end != -1 and json_end > json_start:
        try:
            data = _json.loads(text[json_start:json_end + 1])
            if isinstance(data, dict) and "分镜列表" in data:
                return data
        except _json.JSONDecodeError:
            pass
    
    # 尝试3：寻找 ```json 块
    md_match = re.search(r'```(?:json)?\s*\n?(.*?)\n?```', text, re.DOTALL)
    if md_match:
        try:
            data = _json.loads(md_match.group(1).strip())
            if isinstance(data, dict) and "分镜列表" in data:
                return data
        except _json.JSONDecodeError:
            pass
    
    return None


# ═════════════════════════════════════════════════════════════════════════════
# 核心适配器：供 ui_production.py 分块调用
# ═════════════════════════════════════════════════════════════════════════════

def _parse_timecode_end(tc: str) -> float:
    """解析时间码字符串的结束秒数（用于跨切块时间码累积）。"""
    parts = tc.split('~')
    if len(parts) < 2:
        return 0.0
    end_part = parts[-1].strip()
    end_clean = re.sub(r'[+＋]\s*\d+.*$', '', end_part).strip()
    
    match = re.match(r'(\d{1,2}):(\d{2}):(\d{2})', end_clean)
    if match:
        return int(match.group(1)) * 3600 + int(match.group(2)) * 60 + int(match.group(3))
    
    match = re.match(r'(\d{1,2}):(\d{2})', end_clean)
    if match:
        return int(match.group(1)) * 60 + int(match.group(2))
    
    return 0.0


def _validate_min_content(shot_list: list):
    """代码层标注画面内容过短的镜头（v4.0: 适配四段式格式解析）。遍历全部镜头，逐个检查。"""
    for shot in shot_list:
        prompt = str(shot.get("终极Seedance提示词", ""))
        # 尝试从四段式格式中提取「3. 动态描述」段的内容
        section3_match = re.search(r'3\.\s*动态描述\s*\n(.+?)(?:\n\n4\.|\Z)', prompt, re.DOTALL)
        if section3_match:
            section3_text = section3_match.group(1).strip()
            # 去掉镜头参数行，取纯画面描述
            # 镜头参数行格式: "@场景名\n\n景别：xxx，机位：xxx，构图：xxx，运镜：xxx。\n..."
            pure_content = re.sub(r'^@\S+\s*\n+', '', section3_text)
            pure_content = re.sub(r'^[^\n]*?(?:景别|机位|构图|运镜)[^\n]*?\n', '', pure_content)
            pure_content = pure_content.strip()
            if len(pure_content) < 50:
                shot["终极Seedance提示词"] = prompt + f"\n\n⚠️ [画面描述仅{len(pure_content)}字，可能不够丰富]"
            continue

        # 回退：兼容旧格式（v3.0 扁平提示词）
        content_match = re.search(r'画面内容：(.+)$', prompt, re.DOTALL)
        if content_match:
            content_text = content_match.group(1).strip()
            if len(content_text) < 50:
                shot["终极Seedance提示词"] = prompt + f"\n⚠️[画面描述仅{len(content_text)}字，可能不够丰富]"


def run_crew_on_chunk(
    chunk: str,
    global_chars: str,
    style_tokens: str,
    engine_choice: str,
    api_base: str,
    api_key: str,
    model_name: str,
    time_offset_seconds: float = 0.0,
    provider: str = "",
    timeout: int = 600,
    qa_api_base: str = "",
    qa_api_key: str = "",
    qa_model_name: str = "",
    qa_provider: str = "",
    qa_timeout: int = 600,
    max_tokens: int = 8000,
    fallback_api_base: str = "",
    fallback_api_key: str = "",
    fallback_model_name: str = "",
    fallback_provider: str = "",
    fallback_timeout: int = 600,
) -> tuple:
    """
    v3.0: 对单个剧本切块运行 2-Agent 工作流（Director → QA），
    然后由代码层组装 Seedance 提示词。

    新增参数：
      - timeout: Director Agent 的 LLM 超时（秒）
      - qa_api_base / qa_api_key / qa_model_name / qa_provider / qa_timeout:
        QA Agent 独立模型配置。若未提供，QA 回退到与 Director 相同配置。
      - max_tokens: 单次 LLM 输出上限，防止长输出被服务商静默截断。
      - fallback_* : Director 跨服务商降级配置。某服务商不可用时切到备用服务商重试，
        而不是用同一个（可能超时的）模型死循环。

    返回 (list of dict, total_seconds_float, global_atmosphere_str)：
      - list: 每个 dict 含 4 列（镜头号/时间码/景别机位运镜/终极Seedance提示词）
      - total_seconds: 本切块累计总时长（秒）
      - global_atmosphere: 全局氛围画质文本（首个切块的展示给用户）
    """
    # ── 构建输入 ──
    chars_prefix = f"{global_chars}\n\n" if global_chars.strip() else ""
    
    if time_offset_seconds > 0:
        offset_min = int(time_offset_seconds // 60)
        offset_sec = int(time_offset_seconds % 60)
        time_hint = (
            f"⚠️ 本切块不是剧本开头！前序已累计 {offset_min} 分 {offset_sec} 秒。"
            f"请从 {offset_min:02d}:{offset_sec:02d} 开始分配时间码。\n\n"
        )
    else:
        time_hint = ""
    
    script_input = f"{time_hint}{chars_prefix}{chunk}"
    effective_style = style_tokens.strip() if style_tokens.strip() else "（自动模式：完全根据剧本内容推断视觉风格）"
    
    # ── 代码预提取：角色列表 ──
    char_injection = ""
    try:
        from shared.script_preprocessor import generate_character_list_for_storyboard
        character_list_text = generate_character_list_for_storyboard(script_input)
        if character_list_text and "检测到角色" not in character_list_text:
            char_injection = f"""
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【角色列表 — 代码已预提取】
{character_list_text}
注意：以上角色由代码自动提取，请直接使用。如有遗漏请自行补充。
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
"""
    except Exception:
        pass
    
    # ── 代码预计算：体量分析 ──
    duration_guide = ""
    try:
        from shared.script_preprocessor import generate_duration_guide
        duration_guide = generate_duration_guide(script_input)
    except Exception:
        pass
    
    # ── 构建主用 LLM（Director + QA 各自独立，均显式限制 max_tokens 防截断）──
    director_llm = create_llm(
        engine_choice, api_base, api_key, model_name,
        provider=provider, timeout=timeout, max_tokens=max_tokens
    )
    if qa_model_name and qa_provider:
        effective_qa_api_base = qa_api_base or api_base
        effective_qa_api_key = qa_api_key or api_key
        qa_llm = create_llm(
            engine_choice,
            effective_qa_api_base,
            effective_qa_api_key,
            qa_model_name,
            provider=qa_provider,
            timeout=qa_timeout,
            max_tokens=max_tokens,
        )
        logger.info(
            f"[run_crew_on_chunk] Director={provider}/{model_name} timeout={timeout}s | "
            f"QA={qa_provider}/{qa_model_name} timeout={qa_timeout}s"
        )
    else:
        qa_llm = director_llm
        logger.info(
            f"[run_crew_on_chunk] Director/QA 共用 {provider}/{model_name} timeout={timeout}s"
        )

    # ── 构建跨服务商降级 LLM（deepseek 不可用时切 kimi/glm，避免用同一模型死循环）──
    fallback_llm = None
    if fallback_model_name and fallback_provider:
        fallback_llm = create_llm(
            engine_choice,
            fallback_api_base or api_base,
            fallback_api_key or api_key,
            fallback_model_name,
            provider=fallback_provider,
            timeout=fallback_timeout,
            max_tokens=max_tokens,
        )
        logger.info(
            f"[run_crew_on_chunk] 已配置跨服务商降级: {fallback_provider}/{fallback_model_name} "
            f"timeout={fallback_timeout}s"
        )

    # ── 多层级重试：每次失败都换一组真正不同的 LLM，而非用同一个超时的模型 ──
    # 层级1：完整 Crew（Director + QA），主用模型
    # 层级2：仅 Director（跳过 QA），主用模型
    # 层级3：仅 Director（跳过 QA），跨服务商降级模型
    attempts = [
        (director_llm, qa_llm, False, "primary-full-crew"),
    ]
    if fallback_llm is not None:
        # fallback 只跑 Director，省一次长上下文调用
        attempts.append((fallback_llm, fallback_llm, True, f"fallback-{fallback_provider}-director-only"))
    attempts.append((director_llm, director_llm, True, "primary-director-only"))

    result = None
    used_fallback = False
    last_err = None
    for d_llm, q_llm, director_only, label in attempts:
        try:
            agents = create_agents(d_llm, q_llm)
            tasks = create_tasks(agents)
            crew = create_crew_director_only(agents, tasks) if director_only else create_crew(agents, tasks)
            result = crew.kickoff(inputs={
                'script': script_input,
                'style': effective_style,
                'char_injection': char_injection,
                'duration_guide': duration_guide
            })
            used_fallback = (label != "primary-full-crew")
            logger.info(f"[run_crew_on_chunk] 尝试成功 [{label}]")
            break
        except Exception as e:
            last_err = e
            logger.warning(f"[run_crew_on_chunk] 尝试失败 [{label}]: {type(e).__name__}: {e}")
            continue

    if result is None:
        logger.error(f"[run_crew_on_chunk] 所有重试均失败，最后错误: {last_err}", exc_info=True)
        return [], 0.0, ""

    # ── 提取输出 ──
    qa_raw = ""
    director_raw = ""
    if hasattr(result, 'tasks_output') and result.tasks_output:
        # 完整 Crew：最后一个 task 是 QA
        director_raw = result.tasks_output[0].raw if len(result.tasks_output) >= 1 else ""
        qa_raw = result.tasks_output[-1].raw if len(result.tasks_output) >= 2 else director_raw
    elif hasattr(result, 'raw'):
        qa_raw = result.raw
    else:
        qa_raw = str(result)

    if not qa_raw and not director_raw:
        return [], 0.0, ""

    # ── 解析结构化 JSON ──
    shot_data = parse_structured_json(qa_raw)
    if not shot_data and director_raw:
        # 回退：尝试从 Director 输出解析（如果 QA 输出不可解析）
        shot_data = parse_structured_json(director_raw)

    if not shot_data:
        return [], 0.0, ""

    # ── 代码组装：结构化 JSON → 4列分镜数据 ──
    shot_list, total_seconds, global_atmosphere = assemble_seedance_prompt(
        shot_data, time_offset_seconds
    )

    # ── 代码后验证 ──
    _validate_min_content(shot_list)

    if used_fallback:
        logger.warning(
            f"[run_crew_on_chunk] 已使用降级结果（跳过完整 Crew）：{len(shot_list)}个镜头"
        )

    return shot_list, total_seconds, global_atmosphere


# ═════════════════════════════════════════════════════════════════════════════
# 完整流程入口（独立测试用）
# ═════════════════════════════════════════════════════════════════════════════

def run_production_pipeline(
    style_context: str,
    script_content: str,
    engine_choice: str = "云端API",
    api_base: str = "https://api.deepseek.com",
    api_key: str = "",
    model_name: str = "deepseek-v4-pro",
    output_file: str = '分镜矩阵_Seedance2.0.csv',
    parallel: bool = False,
    provider: str = "deepseek",
    timeout: int = 600,
    qa_api_base: str = "",
    qa_api_key: str = "",
    qa_model_name: str = "",
    qa_provider: str = "",
    qa_timeout: int = 600,
):
    """
    v3.0：完整流程入口（2-Agent + 代码组装）。
    返回: (display_data, csv_path, log_str)
    """
    try:
        if not script_content or not script_content.strip():
            return [], "错误：请输入剧本内容！", None
        
        resolved_api_key = api_key.strip() if api_key.strip() else os.environ.get("DEEPSEEK_API_KEY", "")
        effective_style = style_context.strip() if style_context.strip() else "（自动模式：完全根据剧本内容推断视觉风格）"
        
        log_capture = io.StringIO()
        print(f"\n{'='*60}")
        print(f"Seedance 2.0 结构参数化分镜工作流 v3.0")
        print(f"引擎: {engine_choice} | 模型: {model_name}")
        print(f"剧本长度: {len(script_content)} 字符")
        print(f"{'='*60}\n")
        
        # ── 代码预计算 ──
        duration_guide = ""
        try:
            from shared.script_preprocessor import generate_duration_guide
            duration_guide = generate_duration_guide(script_content)
        except Exception:
            pass
        
        director_llm = create_llm(
            engine_choice, api_base, resolved_api_key, model_name, provider=provider, timeout=timeout
        )

        # QA 模型：未单独配置时回退到 Director 配置
        if qa_model_name and qa_provider:
            effective_qa_api_base = qa_api_base or api_base
            effective_qa_api_key = qa_api_key or resolved_api_key
            qa_llm = create_llm(
                engine_choice,
                effective_qa_api_base,
                effective_qa_api_key,
                qa_model_name,
                provider=qa_provider,
                timeout=qa_timeout,
            )
        else:
            qa_llm = director_llm

        agents = create_agents(director_llm, qa_llm)
        tasks_obj = create_tasks(agents)
        
        # ── 日志捕获 ──
        class LogCaptureHandler(logging.Handler):
            def __init__(self, capture):
                super().__init__()
                self.capture = capture
            def emit(self, record):
                try:
                    self.capture.write(self.format(record) + '\n')
                except Exception:
                    self.handleError()
        
        root_logger = logging.getLogger()
        root_logger.setLevel(logging.DEBUG)
        root_logger.handlers.clear()
        capture_handler = LogCaptureHandler(log_capture)
        capture_handler.setLevel(logging.DEBUG)
        capture_handler.setFormatter(logging.Formatter('%(message)s'))
        root_logger.addHandler(capture_handler)
        console_handler = logging.StreamHandler(sys.stdout)
        console_handler.setLevel(logging.DEBUG)
        console_handler.setFormatter(logging.Formatter('%(message)s'))
        root_logger.addHandler(console_handler)
        
        try:
            if parallel:
                import threading
                print("\n并行模式：Director → QA‖(后续代码组装)\n")
                
                director_crew = create_crew_director_only(agents, tasks_obj)
                director_result = director_crew.kickoff(inputs={
                    'script': script_content,
                    'style': effective_style,
                    'char_injection': '',
                    'duration_guide': duration_guide
                })
                
                qa_result = [None]
                def run_qa():
                    try:
                        qa_crew = create_crew_qa_only(agents, tasks_obj)
                        qa_result[0] = qa_crew.kickoff(inputs={
                            'script': script_content,
                            'style': effective_style,
                            'char_injection': '',
                            'duration_guide': duration_guide
                        })
                    except Exception as e:
                        print(f"QA Agent 异常: {e}")
                
                t_qa = threading.Thread(target=run_qa, name="crew-qa")
                t_qa.start()
                t_qa.join()
                
                # 合并结果
                from crewai import CrewOutput
                class ParallelCrewOutput:
                    def __init__(self, director, qa):
                        self.tasks_output = []
                        if director and hasattr(director, 'tasks_output'):
                            self.tasks_output.extend(director.tasks_output)
                        if qa and hasattr(qa, 'tasks_output'):
                            self.tasks_output.extend(qa.tasks_output)
                        self.raw = qa.raw if qa and hasattr(qa, 'raw') else str(qa or director)
                result = ParallelCrewOutput(director_result, qa_result[0])
            else:
                print("\n串行模式：Director → QA → 代码组装\n")
                crew = create_crew(agents, tasks_obj)
                result = crew.kickoff(inputs={
                    'script': script_content,
                    'style': effective_style,
                    'char_injection': '',
                    'duration_guide': duration_guide
                })
        finally:
            root_logger.removeHandler(capture_handler)
            root_logger.removeHandler(console_handler)
            capture_handler.close()
            console_handler.close()
        
        captured_log = log_capture.getvalue()
        log_capture.close()
        
        # ── 提取原始输出 ──
        qa_raw = ""
        if hasattr(result, 'tasks_output') and len(result.tasks_output) >= 2:
            qa_raw = result.tasks_output[-1].raw
        elif hasattr(result, 'tasks_output') and len(result.tasks_output) >= 1:
            qa_raw = result.tasks_output[-1].raw
        elif hasattr(result, 'raw'):
            qa_raw = result.raw
        else:
            qa_raw = str(result)
        
        # ── 解析 + 组装 ──
        shot_data = parse_structured_json(qa_raw)
        
        csv_path = output_file
        display_data = []
        
        if shot_data:
            shot_list, total_seconds, global_atmosphere = assemble_seedance_prompt(shot_data)
            _validate_min_content(shot_list)
            
            if shot_list:
                header = ["镜头号", "时间码", "景别机位运镜", "终极Seedance提示词"]
                rows = [[s[h] for h in header] for s in shot_list]
                
                with open(csv_path, 'w', encoding='utf-8-sig', newline='') as f:
                    writer = csv.writer(f)
                    writer.writerow(header)
                    writer.writerows(rows)
                
                total_min = int(total_seconds // 60)
                total_sec = int(total_seconds % 60)
                print(f"\n✓ Seedance 2.0 分镜矩阵已保存: {csv_path} ({len(rows)} 镜 / {total_min}分{total_sec}秒)")
                display_data = [header] + rows
            else:
                print("\n⚠️ 组装后无有效分镜数据")
        else:
            fallback_path = '分镜_原始输出.txt'
            with open(fallback_path, 'w', encoding='utf-8') as f:
                f.write(qa_raw)
            print(f"\n⚠️ JSON 解析失败，原始输出已保存: {fallback_path}")
        
        return display_data, csv_path if os.path.exists(csv_path) else None, captured_log
    
    except Exception as e:
        error_msg = f"错误:\n{type(e).__name__}: {str(e)}"
        print(error_msg)
        import traceback
        traceback.print_exc()
        return [], error_msg, None
