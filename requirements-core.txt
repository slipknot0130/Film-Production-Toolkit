# =============================================================================
# 核心运行时依赖（打包用）
# 不含 crewai / langchain（分镜工作台可选依赖），避免打包体积暴涨与版本冲突。
# 源码开发者如需完整功能（含分镜工作台），请使用 requirements.txt。
# =============================================================================
streamlit>=1.28.0
openai>=1.12.0
python-dotenv>=1.0.0
requests>=2.31.0
httpx>=0.24.0
pandas>=2.0.0
openpyxl>=3.1.0
python-docx>=0.8.11
pywebview>=4.4.0
