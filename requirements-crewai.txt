# CrewAI 分镜矩阵依赖（可选）
# ─────────────────────────────────────────────────────────────
# 不安装本文件，分镜工作台仍可使用：程序会自动切换至「轻量分镜引擎」
# （OpenAI SDK 直调，无需 CrewAI），正常生成分镜。
#
# 仅在希望启用工业级「CrewAI 4-Agent 矩阵」时才需要安装：
#     pip install -r requirements-crewai.txt
#
# 注：CrewAI 依赖链较重，在 Apple Silicon (M1/M2/M3) 上偶尔会因个别
# 原生子依赖编译失败。若安装失败无需担心——轻量分镜引擎不受任何影响。
# ─────────────────────────────────────────────────────────────
crewai>=0.28.0
langchain-openai>=0.1.0
